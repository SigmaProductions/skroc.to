describe('addition', () => {
    it('should work.', () => {
        const a = 1;
        const b = 2;
        const result = a + b;
        expect(result).toEqual(3);
    });
});
//# sourceMappingURL=index.test.js.map